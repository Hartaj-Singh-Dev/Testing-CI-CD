name: My first Github Action
on: [push]
jobs:
    build-app:
       runs-on: ubuntu-latest
       steps:
          - uses: actions/setup-node@v2
            with:
               node-version: '14'
          - run: tree
   